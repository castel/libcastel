#pragma once

namespace castel
{

    namespace utils
    {

        template < typename Return, typename Args... >
        class CFunctionWrapper
        {

        public:

            llvm::V

        };

        template < typename Return, typename... Args >
        runtime::Box * wrapCFunction< Return fn( Args... ) >( int argc, runtime::Box ** argv )
        {
            runtime
        }

    }

}
